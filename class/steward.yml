parameters:
  kapitan:
    compile:
      - input_paths:
          - steward/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - output_path: steward
        input_type: jsonnet
        output_type: yaml
        input_paths:
          - steward/component/main.jsonnet

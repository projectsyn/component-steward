parameters:
  steward:
    namespace: syn
    api_url: https://synventory.syn.vshn.net/
    images:
      steward:
        image: docker.io/vshn/steward
        tag: v0.0.1

  kapitan:
    compile:
      - output_path: steward
        input_type: jsonnet
        output_type: yaml
        input_paths:
          - steward/component/main.jsonnet
